//Enum to store the names of all attributes, which are referenced between multiple classes (Globally).

enum Attributes {
  STRENGTH, WEIGHT, AGILITY, CLIMBING, WARMTH, 
  HUNGRINESS, STEALTH, CUNNING, CLAW_SHARPNESS,
  FLIGHT, SWIMMING
}